<!DOCTYPE html>
<html>
  <head>
    <title>questions</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy
    +HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <link rel="stylesheet" href="/topic.css">
  </head>
  <body style="background-color: whitesmoke;">
    <ul class="total">
      <li>INTERVIEW</li>
      <div class="log">
        <% if(user) { %>
          <li>welcome <%= user.username %></li>
          <li><a href="/logout">logout</a></li>
          <% if(admin) {%>
            <li><a href="/admin">admin panel</a></li>
          <%}%>
        <% } else { %>
            <li><a href="/login">login</a></li>
            <li><a href="/signup">signup</a></li>
         <%}%>
      </div>
  </ul>
    <div class="head" style="text-align: center;">
      <h2>Topics</h1>
    </div>
    <div class="d-flex flex-wrap justify-content-between flex-row" style="padding-top: 50px;
    padding-left: 70px;padding-right: 80px;">
    <% if (result.length > 0) { %>
      <% result.forEach((topic) => { %>
        <div class="topic" style="height: 310px;border: 5px solid white;width: 310px;margin-bottom: 50px;margin-right: 0px;box-shadow:5px 5px 4px 5px #888888;">
          <a class="single" href="/questions/<%= topic.section %>" >
            <img src="<%= topic.image %>" width="305px" height="255px">
            <div class="topic-tittle"  style="text-align: center;background-color: white;height: 50px;padding-top: 10px;" ><%= topic.section %></div>
          </a>
        </div>
      <% }) %>
    <% } else { %>
      <p>There are no topics to display...</p>
    <% } %>
    </div>
  </body>
</html>