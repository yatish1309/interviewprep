<!DOCTYPE html>
<html>
  <head>
    <title>questions</title>
  </head>
  <body>
    <p>welcome</p>
    <% if (result.length > 0) { %>
      <% result.forEach((ref) => { %>
        <a class="single" href="<%= ref.link %>">
          <h3 class="title"><%= ref.topic %></h3>
        </a>
      <% }) %>
    <% } else { %>
      <p>There are no topics to display...</p>
    <% } %>
  </body>
</html>